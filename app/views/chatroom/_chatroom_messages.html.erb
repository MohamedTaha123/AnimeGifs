<div id="messages" data-chatroom-id="<%= @chatroom.id %>">
  <% if @chatroom.messages.present? %>
    
      <% @chatroom.messages.order(:created_at).includes([:user]).each do |message| %>
          <%= render "messages/message", message: message %>
      <% end %>
   
  <% end %>
</div>
