<h1>New Chat</h1>
<% if @chats.present? %>
    <% @chats.each do |chat| %>
        <p id="<%=chat.id%>"><%= chat.message %></p>
    <% end %>
<% end %>
<div id="new_message"></div>
<%= render 'form', chat: @chat %>

