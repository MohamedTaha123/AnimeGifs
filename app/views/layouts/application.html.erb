<!DOCTYPE html>
<html>

<head>
  <%= render 'shared/head' %>
  <%= render 'shared/notify'%>
</head>

<body>
  <%= render 'shared/navbar' %>
  <div class="notifications bottom-right">
      <%= render 'shared/notices' %>
  </div>
 
  <div class="container">
      <%= yield %>

  </div>

   
</body>

</html>
