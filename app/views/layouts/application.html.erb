<!DOCTYPE html>
<html>

<head>
  <%= render 'shared/head' %>
  <%= render 'shared/notify'%>
</head>

<body>
  <%= render 'shared/navbar' %>
  
  <div class="container">
    
     <%#= render 'shared/back' %>
      
      <%= yield %>
     <div class="notifications bottom-right">
        <%= render 'shared/notices' %>
      </div>
  </div>

   
</body>

</html>
